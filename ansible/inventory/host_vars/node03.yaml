---
ansible_host: "{{ lookup('community.general.onepassword', 'cluster-network', field='NODE_IP_3') }}"
talos_mac: "{{ lookup('community.general.onepassword', 'cluster-network', field='NODE03_MAC') }}"
